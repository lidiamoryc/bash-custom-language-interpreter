# Variable declaration
int_var n = 10

# Function declaration for factorial
function factorial(int_var n) {
    if (n == 1) {
        return 1
    } else {
        return $((
            n * factorial($((n-1)))
        ))
    }
}

echo factorial(n)
